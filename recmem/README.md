# RecMem: Adaptive Memory System

Self-healing memory architecture for the Cognee hackathon.

## Setup
1. `pip install -e .`
2. `cp .env.example .env` (add your API keys)
3. `python -m recmem.server`
4. Open `http://localhost:5000`

## Run tests
`pytest tests/`